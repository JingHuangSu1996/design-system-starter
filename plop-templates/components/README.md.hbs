# {{ pascalCase name }}


## Install

| type | command                                        |
| ---- | ---------------------------------------------- |
| npm  | `npm install @your-design-system/{{{ dashCase name }}}` |
| yarn | `yarn add @your-design-system/{{{ dashCase name }}}`    |
| pnpm | `pnpm add @your-design-system/{{{ dashCase name }}}`    |


## Usage

```jsx
import {{{ pascalCase name }}} from `@your-design-system/{{{ dashCase name }}}`;

function App() {
	return (
		<{{ pascalCase name }} />
	);
}
```
